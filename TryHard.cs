using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Runtime.InteropServices;
using System.Web;
using System.Net;

// From https://github.com/SimonCropp/TextCopy
namespace AoC
{
   internal static class TryHard
   {
      /*
      public static async string GetInput( int year, int day )
      {
         string filename = $"{year}/inputs/{day:00}.txt"; 
         string url = $"https://adventofcode.com/{year}/day/{day}/input";

         if (File.Exists(filename)) {
            Util.ReadFileToString(filename); 
         } else {
            HttpClient client = new HttpClient(); 

            CookieContainer cookies = new CookieContainer(); 
            

            var res = client.GetAsync(url); 
            await res; // just block on it. 




            
         }
      }
      */
   }
}
